<header class="cf">
  <p id="header-img"><%= image_tag "music_record_disc.png" %></p>
  <% if logged_in? %>
    <p id="user-name"><%= current_user.name %>さん</p>
  <% end %>
</header>

<% flash.each do |key, value| %>
  <p><%= content_tag(:div, value, class: "#{key}") %></p>
<% end %>